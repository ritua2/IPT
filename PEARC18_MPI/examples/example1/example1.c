#include <stdio.h>
//this signature of function main is not required but used deliberately here to illustrate a point
int main(int argc, char *argv[])
{
   // printf() displays the string inside quotation
      printf("Wonderful Class!");
      return 0;
}
